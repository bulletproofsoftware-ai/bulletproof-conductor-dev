project: "hermes-inspired-enhancements"
generated_at: "2026-05-20T20:00:00Z"
cold_start: true
note: "Insufficient data — fewer than 10 Gemini validations available in the analysis window. Only 9 validations present (7 PASS + 2 PARTIAL→PASS). Extraction requires ≥10. Cold-start guard triggered; phrase correlation computation skipped. File populated with structured metadata for future aggregate runs."
window:
  validations_analyzed: 9
  pass_count: 7
  fail_count: 0
  partial_count: 2
  partial_then_pass_count: 2
  baseline_pass_rate: 0.78
  scope: "current_workflow"
  workflow_id: "hermes-inspired-enhancements"
  workflow_tier: "MAJOR"
  analysis_date: "2026-05-20"
  compare_workflows_supplied: false
ku_phrases: []
ki_phrases: []
# Qualitative observations preserved for future aggregate extraction:
# These are NOT statistically derived (n<10); they are analyst notes for human review.
qualitative_notes:
  candidate_ku_signals:
    - phrase: "explicit acceptance criteria"
      observation: "All 4 QA dispatches that included numbered acceptance criteria in their prompts produced structured PASS/FAIL per-AC verdicts, enabling fast triage. The two PARTIAL validations (E6 denylist gap, E3 awk bug) were caught because acceptance criteria made the gaps unambiguous."
      basis: "gv_20260520_e6 (PARTIAL→PASS on explicit AC-E6 list), gv_20260520_e1_retroactive (PASS on AC-E1-1 through AC-E1-10 enumeration)"
    - phrase: "retroactive validation per CLAUDE.md §8"
      observation: "The retroactive-validation pattern (when session crash loses dispatch arrays) produced a clean 100% PASS. Having a documented fallback procedure prevented the crash from becoming a compliance gap."
      basis: "gv_20260520_e1_retroactive"
    - phrase: "inline fix then revalidate"
      observation: "Inline fix + targeted revalidation on the same dispatch (rather than full re-dispatch) resolved PARTIAL→PASS in both cases without triggering a re-dispatch counter increment. Efficient remediation path."
      basis: "gv_20260520_e6 (denylist gap fixed inline), E3 (awk extraction + schema nesting fixed inline)"
  candidate_ki_signals:
    - phrase: "combined dispatch"
      observation: "The combined E3+E5 validation dispatch (gv_20260520_e5_e3_combined) validated two independent enhancements in one call. While efficient, the combined prompt made it harder to attribute finding severity to a specific enhancement. Future workflows should consider per-enhancement validation even when dispatching combined implementation."
      basis: "P2.B+P4.A combined validate"
    - phrase: "QA test plan deviation"
      observation: "E2 QA used a custom test plan that differed from the spec ACs, producing two false FAILs. Prompts that do not anchor QA explicitly to the spec AC list tend to produce plan-vs-spec drift findings."
      basis: "P2.A-E2-qa CONDITIONAL PASS 6/8 — FAILs were not spec violations"
